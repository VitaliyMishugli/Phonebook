import { createTheme } from "@mui/material";

export const theme = createTheme( Object.freeze({
  palette: {
    primary: {
      main: '#ff4000',
    }, 
    secondary: {
      main: '#ebe2dd'
    },
    mode: 'light'
  }
}))